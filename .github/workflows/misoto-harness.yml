# Generated by Misoto Harness; DO NOT EDIT. version=0.5.0 source=8addea93f584 profiles=docs content=877a356d3fcc6c39
# Every byte of this file is hashed in .rulesync/managed-files.json, so a
# repository formatter must not rewrite it; the next line keeps Prettier off.
# prettier-ignore
name: Misoto Harness

on:
  workflow_call:

permissions:
  contents: read

jobs:
  rules:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          # The rulesync toolchain installs through pnpm below; leave the consumer
          # repository's own package manager cache alone.
          package-manager-cache: false
      - uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0
        with:
          package_json_file: .rulesync/package.json
      - run: python .rulesync/harness-check.py
      - run: pnpm --dir .rulesync install --ignore-workspace --frozen-lockfile
      - run: ./.rulesync/node_modules/.bin/rulesync generate --check --silent
